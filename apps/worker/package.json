{
  "name": "@attendance/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@attendance/attendance-core": "workspace:*",
    "@attendance/db": "workspace:*",
    "@attendance/shared": "workspace:*",
    "node-cron": "^4.6.0",
    "nodemailer": "^9.0.5",
    "pino": "^10.3.1",
    "tsx": "^4.23.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
